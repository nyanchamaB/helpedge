"use client";
//general system settings page: managing application-wide settings, integrations, and configurations.
import { redirect } from "next/navigation";
export default function SettingsPage() {
  redirect("/settings/profile");
}